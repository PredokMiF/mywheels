{
  "name": "@mywheels/root",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "wsrun --parallel start",
    "build": "yarn workspaces run build",
    "serve": "wsrun --parallel serve"
  },
  "devDependencies": {
    "concurrently": "6.0.0",
    "wsrun": "5.2.4"
  }
}
